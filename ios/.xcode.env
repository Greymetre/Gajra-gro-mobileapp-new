# Xcode does not load the interactive shell profile, so `command -v node`
# resolves to an empty value when the app is launched from Finder.
export NODE_BINARY="/Users/mac/.local/node-v22.18.0/bin/node"
